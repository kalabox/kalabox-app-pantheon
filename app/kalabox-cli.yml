cli:
  image: kalabox/cli:dev
  dns:
    - 8.8.8.8
  volumes:
    - $KALABOX_APP_ROOT_BIND:/src:rw
  environment:
    TERM: xterm-color
  links:
    - db:database
  working_dir: /src/code
  stdin_open: true
  tty: true
